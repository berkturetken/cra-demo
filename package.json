{
  "name": "cra-demo",
  "version": "1.0.0",
  "description": "Small demo API used to illustrate CRA / agentic-development risks (CI, SBOM, security gates).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src test",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "eslint": "^9.9.0"
  }
}
